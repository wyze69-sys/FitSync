# FitSync Docs

This folder stores project notes for the JavaScript, Express, and MySQL version of FitSync.

Use `database/schema.sql` to create the local MySQL schema and `database/seed.sql` for starter category data. The backend also seeds demo accounts and sample records on startup when they are missing.
