# FitSync Docs

This folder stores short project notes for the JavaScript, Express, React, and MySQL version of FitSync.

Use `database/schema.sql` to create the local MySQL schema and `database/seed.sql` for starter exercise categories. The backend can also create missing tables and starter demo records when it starts.
