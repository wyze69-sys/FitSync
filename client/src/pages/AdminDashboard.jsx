import AdminPortalView from "../components/AdminPortalView.jsx";

/**
 * Admin landing page. Rendered inside AdminLayout (which provides the nav/footer).
 */
export default function AdminDashboard() {
  return <AdminPortalView />;
}
